# TradeBinder Health Report — 2026-09-26

## What changed since yesterday

Nothing. Zero code commits since 2026-09-10 — 16th day in a row.
Same 4 issues, still open, still unfixed. Build still green.

## Needs attention (unresolved, unchanged)

1. **Next.js critical security advisory — `package.json`**
   Still on `next@16.2.5`. Fix (`16.3.6`) is within the existing `^16.0.0` range but the
   lockfile hasn't been bumped. Today's audit spells out the risk more clearly than
   before: unauthenticated RCE in the Image Optimization API (AVIF) and on
   Windows-hosted servers, plus a middleware/proxy bypass. Fix: `npm install next@16.3.6`
   + commit the updated lockfile. 16 days untouched.

2. **`user_cards` table has no migration — `supabase/migrations/`**
   Still missing a `CREATE TABLE` for `user_cards`; existing migrations only alter it.
   Fresh Supabase setups will break.

3. **`postcss` high-severity advisory — `package.json`**
   Still on `8.5.10`; fix is a minor bump to `8.5.28`. Ships free alongside the Next.js
   bump above.

4. **Debug test page live at `/bortytest` — `app/bortytest/page.tsx`**
   Unauthenticated eBay-price-API test route still shipping in production (confirmed
   again in today's build output). Delete the folder or gate it behind a dev-only check.

## Fine / no change

Build passes clean (16 routes, no TS errors). No new branches (only `main`), no new
TODOs/dead code. Same large files as before (`trade/page.tsx` 1419,
`inbox/[conversationId]/page.tsx` 951, `navbar.tsx` 831) — none grew. Schema otherwise
matches code. No new major dependency gaps beyond the already-known TypeScript 7 /
ESLint 10 (dev tooling only, not urgent). Dev-tooling vulnerabilities from `wrangler`'s
dependency chain (esbuild, undici, ws, etc.) are pre-existing and not shipped to
production.
