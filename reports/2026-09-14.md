# TradeBinder Health Report — 2026-09-14

## What changed since yesterday

Nothing. Zero commits on `main` since 2026-09-10 — 4th day in a row with no code changes.
Same issues, still open, still unfixed.

## Needs attention (unresolved, unchanged)

1. **Next.js critical security advisory — `package.json`**
   Still on `next@16.2.5`. Fix (`16.3.5`) is within the existing `^16.0.0` range but the
   lockfile hasn't been bumped, so `npm ci` keeps installing the vulnerable version.
   Advisory list for 16.2.5 includes an unauthenticated RCE (Windows-hosted servers,
   AVIF image optimization) and a middleware/proxy auth-bypass — critical severity.
   Fix: `npm install next@16.3.5` + commit the updated lockfile. 4 days untouched.

2. **`user_cards` table has no migration — `supabase/migrations/`**
   Still missing a `CREATE TABLE` for `user_cards`; 007/008/009 only alter it, and 5
   files in `app/` query it. Fresh Supabase setups will break.

3. **`postcss` advisory — `package.json`**
   Still on `8.5.10`; fix is a minor bump to `8.5.28`. Low risk, easy fix, ships free
   alongside the Next.js bump above.

## Fine / no change

Build passes clean (14 routes, no TS errors). No new branches (only `main` exists), no
new TODOs/dead code. Same three large files as before (`trade/page.tsx` 1419,
`inbox/[conversationId]/page.tsx` 951, `navbar.tsx` 831) — none grew. Schema otherwise
matches code.
