# TradeBinder Health Report — 2026-09-20

## What changed since yesterday

Nothing on `main` — 10th day in a row with no code changes (only daily report commits).
Same 3 issues, still open, still unfixed.

One new finding today (not code rot, just newly noticed): a leftover debug page at
`app/bortytest/page.tsx` is live in production.

## Needs attention (ranked)

1. **Next.js critical security advisory — `package.json`**
   Still on `next@16.2.5`. Fix (`16.3.5`+) is within the existing `^16.0.0` range but the
   lockfile hasn't been bumped. Fix: `npm install next@16.3.5` + commit updated lockfile.
   10 days untouched.

2. **`user_cards` table has no migration — `supabase/migrations/`**
   Still missing a `CREATE TABLE` for `user_cards`; existing migrations only alter it.
   Fresh Supabase setups will break.

3. **`postcss` high-severity advisory — `package.json`**
   Still on `8.5.10`; fix is a minor bump to `8.5.28`. Ships free alongside the Next.js
   bump above.

4. **Debug test page live at `/bortytest` — `app/bortytest/page.tsx`**
   A manual eBay-price-API test page (hardcoded Charizard ex test card, raw fetch/debug
   UI) is a real, unauthenticated route on the live site. Not new code, just never
   flagged before. Low risk but easy cleanup: delete the folder or gate it behind a
   dev-only check.

## Fine / no change

Build passes clean (17 routes, no TS errors). No new branches (only `main`). No new
TODOs/dead code. Same large files as before (`trade/page.tsx` 1419,
`inbox/[conversationId]/page.tsx` 951, `navbar.tsx` 831) — none grew. Schema otherwise
matches code.
